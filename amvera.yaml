meta:
  name: goal-tracker
  toolchain:
    name: node
    version: 20
build:
  install: npm install && cd frontend && npm install && npm run build
  command: echo "Build complete"
run:
  command: npx serve -s frontend/dist -l 80
